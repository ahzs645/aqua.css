# Fonts (Optional)

aqua.css ships with system font defaults. If you want to bundle custom Aqua-era fonts,
place the files in this folder and update `src/_fonts.scss` to reference them.

Note: Only add font files you are licensed to redistribute.
